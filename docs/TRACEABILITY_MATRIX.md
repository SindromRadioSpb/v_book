# Traceability Matrix

| Problem | Requirement | Code Area | Planned Verification | Docs |
|---|---|---|---|---|
| Silent persisted fallback to Mock | Persisted processing must not silently fall back | `app/services/process_service.py`, `app/ui/documents_view.py` | `tests/test_process_service_nlp_runtime.py`, `tests/test_documents_engine_readiness.py` | `IMPLEMENTATION_LEDGER.md`, `HANDOFF_STATUS.md` |
| User cannot tell expected vs actual engine | Split configured vs effective engine | `app/services/process_service.py`, `app/services/nlp_runtime/` | `tests/test_process_service_nlp_runtime.py` | `DECISIONS_LOG.md`, `CONTEXT_STATE.md` |
| Readiness is shallow and misleading | Introduce runtime diagnostics truth | `app/ui/workers.py`, `app/services/nlp_runtime/` | `tests/test_documents_engine_readiness.py` | `IMPLEMENTATION_LEDGER.md` |
| No audit-grade provenance per run | Add run-level NLP provenance | `app/services/process_service.py` | `tests/test_process_service_nlp_runtime.py`, `tests/test_process_run_state_foundation.py` | `HANDOFF_STATUS.md` |
| NLP invisible in health/resources UX | Expose NLP in Health Check and Resources Manager | `app/services/health_check_service.py`, `app/ui/resources_manager_dialog.py` | `tests/test_health_check_service.py`, `tests/test_resources_manager_dialog.py` | `CONTEXT_STATE.md`, `HANDOFF_STATUS.md` |
| Documents has no obvious remediation path | Add explicit diagnose/setup actions in Documents | `app/ui/documents_view.py` | `tests/test_documents_engine_readiness.py` | `IMPLEMENTATION_LEDGER.md`, `HANDOFF_STATUS.md` |
| Runtime diagnostics can crash on hostile Torch state | Isolate runtime probe in a subprocess and return machine-readable failure codes | `app/services/nlp_runtime/runtime_probe.py` | `tests/test_nlp_runtime_probe.py`, `tests/test_resources_manager_dialog.py`, `tests/test_health_check_service.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Repair guidance is ambiguous between dev and packaged modes | Make remediation and setup steps packaging-aware | `app/services/nlp_runtime/runtime_probe.py`, `app/ui/resources_manager_dialog.py` | `tests/test_nlp_runtime_probe.py`, `tests/test_resources_manager_dialog.py` | `CONTEXT_STATE.md`, `HANDOFF_STATUS.md` |
| Users conflate runtime failures with missing Hebrew model files | Split external runtime dependency from managed Hebrew resource in UX | `app/ui/resources_manager_dialog.py`, `app/ui/documents_view.py`, `app/services/health_check_service.py` | `tests/test_resources_manager_dialog.py`, `tests/test_documents_engine_readiness.py`, `tests/test_health_check_service.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Run provenance is hard to filter and audit | Promote runtime provenance into dedicated `processor_run` fields while keeping the legacy note envelope as fallback | `app/services/process_service.py`, `app/infra/sa_models.py`, `app/infra/migrations/052_processor_run_runtime_provenance.sql`, `scripts/release_smoke_nlp_runtime.py` | `tests/test_process_service_nlp_runtime.py`, `tests/test_process_batch_run_state.py`, `tests/test_process_run_state_foundation.py`, `tests/test_release_smoke_nlp_runtime.py` | `DECISIONS_LOG.md`, `HANDOFF_STATUS.md`, `IMPLEMENTATION_LEDGER.md` |
| Guided repair steps drift across UI surfaces | Reuse one guided repair plan derived from runtime taxonomy | `app/services/nlp_runtime/runtime_probe.py`, `app/ui/documents_view.py`, `app/services/health_check_service.py`, `app/ui/resources_manager_dialog.py` | `tests/test_nlp_runtime_probe.py`, `tests/test_documents_engine_readiness.py`, `tests/test_health_check_service.py`, `tests/test_resources_manager_dialog.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Subprocess probe can disagree with live engine init | Convert live init failures into the same runtime-block contract | `app/services/process_service.py` | `tests/test_process_service_nlp_runtime.py` | `IMPLEMENTATION_LEDGER.md`, `CONTEXT_STATE.md` |
| Documents re-process can still dead-end after a managed runtime-block | Offer a guided recovery router from the main-thread error flow and preserve the machine-readable runtime-block text | `app/ui/workers.py`, `app/ui/documents_view.py` | `tests/test_documents_process_progress_ui.py` | `IMPLEMENTATION_LEDGER.md`, `HANDOFF_STATUS.md` |
| Qt process can still fail to import Torch/Stanza even after guided recovery exists | Recover real Stanza processing via a subprocess-backed engine instead of failing the request | `app/infra/nlp_engines/stanza_engine.py`, `app/infra/nlp_engines/stanza_subprocess_worker.py` | `tests/test_stanza_engine_subprocess.py`, live app-like Qt smoke, live reprocess smoke on DB copy | `IMPLEMENTATION_LEDGER.md`, `CONTEXT_STATE.md` |
| Users expect a local install file in NLP Setup | State explicitly that Python runtime dependencies are external and the Hebrew model is a directory-based managed resource | `app/ui/resources_manager_dialog.py` | `tests/test_resources_manager_dialog.py` | `IMPLEMENTATION_LEDGER.md`, `HANDOFF_STATUS.md` |
| Owner-bound runtime workers can survive dialog/window destruction and crash Qt | Enforce deterministic QThread shutdown on close paths for Documents, Resources Manager, and AppWindow | `app/ui/thread_lifecycle.py`, `app/ui/documents_view.py`, `app/ui/resources_manager_dialog.py`, `app/ui/app_window.py` | `tests/test_documents_process_progress_ui.py`, `tests/test_resources_manager_dialog.py`, `tests/test_workspace_app_window_contract.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Windows packaged/dev app still lacks product ownership of the Stanza subprocess path | Launch worker/probe via `app.main` and bootstrap a managed runtime/model root under app data | `app/main.py`, `app/services/nlp_runtime/managed_runtime.py`, `app/services/nlp_runtime/runtime_probe.py`, `app/infra/nlp_engines/stanza_engine.py` | `tests/test_managed_stanza_runtime.py`, `tests/test_nlp_runtime_probe.py`, `tests/test_stanza_engine_subprocess.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Users still lack one authoritative repair action after the managed runtime exists | Surface one official `Install / Repair NLP Runtime` flow across Documents, Health Check, and Resources Manager | `app/ui/documents_view.py`, `app/services/health_check_service.py`, `app/ui/resources_manager_dialog.py` | `tests/test_documents_engine_readiness.py`, `tests/test_health_check_service.py`, `tests/test_resources_manager_dialog.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Managed runtime can still reuse a broken local Hebrew payload and fail only in the worker startup path | Require full managed payload validity before treating the local copy as ready; otherwise repair from a valid source | `app/services/nlp_runtime/managed_runtime.py` | `tests/test_managed_stanza_runtime.py`, `scripts/release_smoke_nlp_runtime.py --force-hostile-inprocess` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| Windows subprocess runtime can still fail on `torch\lib\c10.dll` without explicit DLL search-path prep | Register Torch/CUDA DLL directories before importing `stanza/torch` in the app-owned probe/worker runtime | `app/infra/nlp_engines/stanza_engine.py`, `app/services/nlp_runtime/stanza_probe_worker.py` | `tests/test_stanza_engine_subprocess.py`, `tests/test_nlp_runtime_probe.py`, `scripts/release_smoke_nlp_runtime.py --force-hostile-inprocess` | `DECISIONS_LOG.md`, `CONTEXT_STATE.md` |
| Unit tests alone do not prove release readiness for Windows managed runtime processing | Add app-like release smoke for hostile in-process launch and DB-copy `Re-process` through managed subprocess `stanza` | `scripts/release_smoke_nlp_runtime.py` | `scripts/release_smoke_nlp_runtime.py --force-hostile-inprocess`, `scripts/release_smoke_nlp_runtime.py --db-path ... --copy-db-to ... --doc-id 1` | `IMPLEMENTATION_LEDGER.md`, `HANDOFF_STATUS.md` |
| `Documents -> Re-process` can still destroy `ProcessWorker` before the underlying thread terminates | Keep business results on `result_ready` and perform worker teardown only from the real `QThread.finished` callback | `app/ui/workers.py`, `app/ui/documents_view.py` | `tests/test_documents_process_progress_ui.py` | `DECISIONS_LOG.md`, `IMPLEMENTATION_LEDGER.md` |
| The original live GUI `Re-process` crash still needed proof on the target machine after the Step 16 hotfix | Confirm the exact project/document repro path succeeds without `QThread` destruction | `app/ui/workers.py`, `app/ui/documents_view.py`, live desktop flow | Live GUI run for project `6`, document `387646`, `Documents -> Re-process` | `IMPLEMENTATION_LEDGER.md`, `HANDOFF_STATUS.md` |
| Release smoke DB validation still depends on heavy source DB copying | Build a tiny document-scoped smoke DB and let `reprocess_document()` rebuild derived state | `scripts/release_smoke_nlp_runtime.py` | `tests/test_release_smoke_nlp_runtime.py`, live `release_smoke_nlp_runtime.py --db-path ... --doc-id 1` | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md` |
| Packaged release still lacks a guaranteed bundled Hebrew payload source | Stage a release-owned Hebrew payload and bundle it into the frozen app | `scripts/stage_stanza_hebrew_payload.py`, `rebuild.ps1`, `hdle_premium_installer.spec` | Build-time staging + packaged smoke in later patches | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md`, `BUILD_WINDOWS_INSTALLER.md` |
| Packaged smoke still cannot prove the Hebrew payload came from the bundled release artifact | Record bundled payload ownership in runtime manifest/probe and prefer bundled packaged payload during bootstrap | `app/services/nlp_runtime/managed_runtime.py`, `app/services/nlp_runtime/runtime_probe.py`, `app/services/nlp_runtime/dto.py` | `tests/test_managed_stanza_runtime.py`, `tests/test_nlp_runtime_probe.py`, packaged smoke in later patch | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md`, `CONTEXT_STATE.md` |
| Bundled payload ownership can still be hidden from operators even if runtime bootstrap knows it | Surface bundled/source ownership in UI, Health Check, Documents details, and smoke output | `app/ui/resources_manager_dialog.py`, `app/services/health_check_service.py`, `app/ui/documents_view.py`, `scripts/release_smoke_nlp_runtime.py` | `tests/test_resources_manager_dialog.py`, `tests/test_health_check_service.py`, `tests/test_documents_engine_readiness.py`, `tests/test_release_smoke_nlp_runtime.py`, release smoke with `--require-source-kind` | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md`, `CONTEXT_STATE.md` |
| Existing managed manifests can carry obsolete generic ownership and break bundled release assertions | Upgrade obsolete manifest ownership to the current bundled source when a valid bundled payload is present | `app/services/nlp_runtime/managed_runtime.py` | `tests/test_managed_stanza_runtime.py`, release smoke with `--require-source-kind bundled_dev` | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md`, `CONTEXT_STATE.md` |

| A stale `dist` artifact can hide whether the packaged build actually contains the bundled Hebrew payload | Rebuild from the real packaging source of truth and verify packaged ownership on a clean managed root | `rebuild.ps1`, `hdle_premium_installer.spec`, `scripts/stage_stanza_hebrew_payload.py`, `app/services/nlp_runtime/managed_runtime.py` | `rebuild.ps1 -SkipFastGates`, packaged `--self-check import`, packaged `--stanza-probe` on clean `HDLE_DATA_ROOT` | `IMPLEMENTATION_LEDGER.md`, `CONTEXT_STATE.md`, `HANDOFF_STATUS.md` |
| Bundled payload ownership alone does not prove packaged Stanza processing is release-ready | Validate frozen Torch/Stanza import separately from bundled ownership | `app/main.py`, `app/services/nlp_runtime/stanza_probe_worker.py`, `app/infra/nlp_engines/stanza_subprocess_worker.py` | packaged `--stanza-probe`, packaged `--stanza-worker` | `DECISIONS_LOG.md`, `HANDOFF_STATUS.md` |
| Frozen packaged probe/worker can still fail even when app-level DLL bootstrap is correct | Run Windows Torch DLL bootstrap from a PyInstaller runtime hook and preflight the critical `c10.dll` chain before user-code imports | `app/infra/runtime_torch_bootstrap.py`, `app/runtime_hooks/pyi_rth_torch_dll_bootstrap.py`, `hdle_premium_installer.spec`, `app/infra/nlp_engines/stanza_engine.py` | `tests/test_stanza_engine_subprocess.py`, packaged `--stanza-probe`, packaged release smoke with `--require-source-kind bundled_packaged`, DB reprocess smoke | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md`, `CONTEXT_STATE.md`, `HANDOFF_STATUS.md` |
| Frozen ONNX helper can still time out before import health is evaluated | Avoid blocking startup on inherited HF cache writability probes; keep existing configured HF cache read-first and fall back only when missing/unusable | `app/tools/onnx_probe.py` | `tests/test_onnx_probe_contract.py`, packaged `HDLE_ONNX_Probe.exe --mode import`, packaged `HDLE_Premium.exe --self-check import`, packaged `HDLE_Premium.exe --self-check health` | `IMPLEMENTATION_LEDGER.md`, `DECISIONS_LOG.md`, `CONTEXT_STATE.md`, `HANDOFF_STATUS.md` |
