Claude Code Prompt

  ROLE
  Ты — senior engineer HDLE Premium (PyQt6 + SQLite WAL/FTS5), отвечаешь за premium UX, минимальный регрессионный риск, deterministic long-ops и сквозную синхронизацию данных.

  GOAL
  Реализовать масштабирование учебного потока на уровне документов:
  1. Доработать вкладку Documents:
  - поиск по названию текста (doc title),
  - новые столбцы/поля: tag, link (кликабельная), level (aleph/bet/gimel/he), topic,
  - фильтрация/сортировка по новым полям.
  2. Добавить отдельную проектную вкладку Sentences:
  - каждая строка = предложение из NLP sentences,
  - переиспользовать существующие инструменты: Translate Selected, Generate Audio, Pronunciation Bootstrap, Play Audio,
  - без UI freeze, через workers + BatchProgressDialogV3.
  3. Доработать TM Panel:
  - фильтр Kind сделать multi-select checklist (по аналогии с гибким фильтром через popup/select dialog),
  - сохранение выбранных kind в рамках сессии и между сессиями.
  4. Обеспечить сквозную консистентность и one-truth подход по translation/niqqud/audio во всех view.
  5. Минимизировать риски регрессий и закрыть слепые зоны до реализации.

  HARD CONSTRAINTS
  - Перед планированием и кодом обязательно прочитать релевантную документацию в репозитории.
  - Никаких предположений: каждый вывод должен быть подтвержден кодом/доками/тестом.
  - Любые долгие операции только worker + signals + V3 progress + cancel.
  - Короткие транзакции, chunking, WAL-safe поведение.
  - Без modal-spam: агрегированные ошибки и финальный summary.
  - Сохранить существующие инварианты source-only audio/pronunciation pipelines и TM truth contracts.
  - Минимальные диффы, patch-series, regression-first.

  MANDATORY PREFLIGHT (BEFORE CODING)
  1. Прочитать docs:
  - docs/**
  - README/ROADMAP/ARCH/SECURITY/RELEASE_NOTES/TASK_*/*_AUDIT/*_REPORT/*_EVIDENCE
  - документы по TM Panel, Documents/NLP, Audio/Pronunciation, User Dictionaries, UI DoD evidence
  - AGENTS.md и skills pack premium_desktop_pyqt_sqlite
  2. Подтвердить entrypoints в коде:
  - app/ui/documents_view.py
  - app/ui/translation_management_panel.py
  - app/ui/workers.py
  - app/ui/dialogs/batch_progress_dialog_v3.py
  - app/ui/models_qt.py
  - app/services/* (document ingest/NLP/translation/audio/pronunciation/user-dictionary/tm)
  - app/infra/sa_models.py + migrations
  3. Закрыть слепые зоны до кода:
  - где хранить metadata документа (source_document vs отдельная таблица),
  - уровни level (фиксированный enum),
  - безопасный контракт link (валидация/whitelist схем),
  - контракт sentence identity и canonical normalization,
  - стратегия kind multi-select persistence и backward compatibility.

  OUTPUT FORMAT (STRICT)
  1) Repo audit findings
  2) Patch plan (PATCH-01..N with files + steps)
  3) Tests (new + regression + commands)
  4) DoD
  5) Commit messages (one per patch)

  Patch Steps

  PATCH-01 — Documentation/Audit Gate (docs-only)
  Files:
  - docs/TASK_SENTENCES_WORKSPACE_AUDIT.md (new)
  - docs/GAP_MATRIX_SENTENCES_TM_DOCUMENTS.md (new)
  Steps:
  - Полная карта docs -> code entrypoints.
  - Зафиксировать unknowns и способы верификации.
  - Подтвердить DB/threading/UI contracts, которые нельзя нарушить.
  Smoke:
  - Н/Д (docs only)
  Commit:
  - docs(audit): map docs-to-code and gap matrix for sentences/documents/tm iteration

  PATCH-02 — DB & DTO for Documents metadata
  Files:
  - app/infra/migrations/0XX_documents_metadata.sql (new)
  - app/infra/sa_models.py
  - app/domain/dto.py
  Steps:
  - Добавить поля к документам: tag, link_url, level, topic.
  - level: CHECK enum ('aleph','bet','gimel','he') + nullable-safe.
  - Индексы для фильтрации/поиска/сортировки.
  - Обновить DTO/модели без ломки существующих API.
  Smoke:
  - миграция на чистой и рабочей БД.
  Commit:
  - feat(db): add document metadata fields (tag/link/level/topic) with indexes and constraints

  PATCH-03 — Documents service logic (search/filter/sort/update)
  Files:
  - app/services/document_service.py (или существующий documents/ingest service)
  - app/services/* where documents are queried
  Steps:
  - Добавить поиск по названию текста и фильтры по новым полям.
  - Безопасный sort allowlist для новых колонок.
  - Валидация link_url (http/https only), length limits, trim/sanitize.
  - Сохранение/редактирование metadata через service layer.
  Smoke:
  - CRUD metadata + фильтры/сортировка.
  Commit:
  - feat(documents): add metadata query/update APIs with safe filter/sort and link validation

  PATCH-04 — Documents UI upgrade (premium)
  Files:
  - app/ui/documents_view.py
  - app/ui/models_qt.py
  - app/ui/table_layout_controller.py (только если нужно)
  Steps:
  - Добавить столбцы Tag/Link/Level/Topic.
  - Реализовать поиск по title + фильтры metadata.
  - Link сделать кликабельным безопасно (no unsafe schemes).
  - Сохранение ширин/порядка колонок и текущих фильтров между сессиями.
  Smoke:
  - UI редактирование metadata, клики по link, перезапуск приложения.
  Commit:
  - feat(ui-documents): add searchable metadata columns and safe clickable links

  PATCH-05 — Sentences workspace data/service layer
  Files:
  - app/services/sentences_workspace_service.py (new)
  - app/domain/dto.py
  - app/services/* integration points
  Steps:
  - Построить paginated list по document_sentence (или подтвержденной таблице).
  - Каждая строка содержит source sentence + overlay translation/niqqud/audio/status.
  - Batch-friendly ID selection для selected/current page/all filtered.
  - Никаких per-row SQL; только batch lookups.
  Smoke:
  - выборка 10k+ предложений без блокировки UI.
  Commit:
  - feat(service): add sentences workspace service with batch overlays and paginated scopes

  PATCH-06 — Sentences workspace UI + actions reuse
  Files:
  - app/ui/sentences_view.py (new)
  - app/ui/project_workspace/* (tab registration)
  - app/ui/workers.py
  - app/ui/dialogs/* (reuse existing dialogs)
  Steps:
  - Новая вкладка Sentences в проекте.
  - Таблица + pagination + filters + safe sorting.
  - Действия: Translate Selected, Generate Audio Selected, Pronunciation Bootstrap Selected, Play Audio.
  - Контекстное меню parity с другими вкладками.
  - Все long-ops через worker + V3 progress + cancel.
  Smoke:
  - end-to-end на selected/current page/all filtered.
  Commit:
  - feat(ui-sentences): add sentence-level training workspace with translation/audio/pronunciation actions

  PATCH-07 — TM Kind multi-select filter (persisted)
  Files:
  - app/ui/translation_management_panel.py
  - app/ui/dialogs/tm_kind_filter_dialog.py (new or reuse)
  - app/services/translation_admin_service.py
  - app/infra/settings.py
  Steps:
  - Заменить single-kind filter на checklist popup/dialog.
  - Сохранять выбранные kind в сессии и между сессиями.
  - Обновить query builder + count + pagination + export compatibility.
  - Сохранить safe-sort allowlist и deterministic behavior.
  Smoke:
  - фильтр kind при перезапуске сохраняется и корректно влияет на выдачу.
  Commit:
  - feat(ui-tm): add persisted multi-select kind filter with safe query integration

  PATCH-08 — Integration hardening + docs evidence
  Files:
  - docs/USER_GUIDE_SENTENCES_WORKSPACE.md (new)
  - docs/UI_DOD_EVIDENCE_SENTENCES_DOCS_TM.md (new)
  - docs/KEYBOARD_SHORTCUTS.md (if updated)
  Steps:
  - Smoke matrix (cold start, filters persistence, batch flows, cancel behavior).
  - Подтвердить отсутствие UI freeze и per-row query hotspots.
  - Зафиксировать known limits и rollback notes.
  Commit:
  - docs(evidence): add DoD smoke matrix for documents metadata, sentences workspace, and tm kind filters

  Tests

  NEW TESTS
  - tests/test_documents_metadata_service.py
    - test_metadata_fields_validation_and_persistence
    - test_search_by_title_and_metadata_filters
    - test_link_validation_http_https_only
  - tests/test_documents_view_metadata_ui.py
    - test_columns_visible_and_editable
    - test_link_click_opens_safe_url_only
    - test_layout_and_filters_persist_between_sessions
  - tests/test_sentences_workspace_service.py
    - test_paginated_sentence_query
    - test_batch_overlay_no_per_row_queries
    - test_selected_current_all_filtered_scope_id_selection
  - tests/test_sentences_workspace_actions.py
    - test_translate_selected_reuses_existing_pipeline
    - test_generate_audio_selected_reuses_existing_pipeline
    - test_pronunciation_bootstrap_selected_reuses_existing_pipeline
    - test_cancel_boundary_consistency
  - tests/test_tm_kind_multiselect_filter.py
    - test_kind_multiselect_filters_results
    - test_kind_selection_persistence_qsettings
    - test_kind_filter_safe_sort_and_counts

  REGRESSION SUITE (MANDATORY)
  - python -m pytest tests/test_tm_panel_ux.py -q
  - python -m pytest tests/test_user_dictionaries_service.py tests/test_user_dictionaries_review_mode.py -q
  - python -m pytest tests/test_audio_generation_service.py tests/test_audio_playback_service.py -q
  - python -m pytest tests/test_pronunciation_bootstrap.py tests/test_audio_generation_with_pronunciation.py -q
  - python -m pytest tests/test_security.py tests/test_task19_tm_global.py tests/test_task19_propagation.py -q

  DoD

  FUNCTIONAL
  - Documents поддерживает title search + metadata columns (tag/link/level/topic) + filter/sort/edit.
  - Новая вкладка Sentences показывает sentence rows и поддерживает Translate/Audio/Pronunciation batch actions.
  - TM Kind filter работает как multi-select checklist с persistence.
  - Сквозная консистентность translation/niqqud/audio между Dictionary/Terms/TM/User Dictionaries/Term Cards/Sentences.

  UX/PERFORMANCE
  - Нет UI freeze при больших наборах данных.
  - Все long-ops через worker + V3 progress + cancel.
  - Пагинация/фильтры/сортировка deterministic.
  - Сохранение widths/layout/filters между сессиями.

  RELIABILITY/SECURITY
  - WAL-safe: chunked writes, short transactions, retry-on-locked.
  - Parameterized SQL, safe sort allowlists.
  - link_url sanitization (no javascript/file execution).

  DOCS/EVIDENCE
  - Полная docs-to-code трассировка.
  - UI DoD evidence с smoke matrix и reproduction steps.
  - Зафиксированы unknowns, принятые решения и ограничения.

  COMMIT MESSAGES (PROPOSED)
  1. docs(audit): map docs-to-code and gap matrix for sentences/documents/tm iteration
  2. feat(db): add document metadata fields (tag/link/level/topic) with indexes and constraints
  3. feat(documents): add metadata query/update APIs with safe filter/sort and link validation
  4. feat(ui-documents): add searchable metadata columns and safe clickable links
  5. feat(service): add sentences workspace service with batch overlays and paginated scopes
  6. feat(ui-sentences): add sentence-level training workspace with translation/audio/pronunciation actions
  7. feat(ui-tm): add persisted multi-select kind filter with safe query integration
  8. docs(evidence): add DoD smoke matrix for documents metadata, sentences workspace, and tm kind filters

  Если хотите, следующим сообщением подготовлю сокращённую версию этого же промта (короткий execution-brief на 1 экран).
